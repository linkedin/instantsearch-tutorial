Start an instance of elastic search
===================================
$ /path/to/elasticsearch

Index posts and tags
====================
Posts (for instant results):
$ python indexing/instant_results.py --i data/stackoverflow/posts.json

Tags (for query autocomplete):
(Adjust number for smaller or larger index)
$ cat data/stackoverflow/tags.xml | grep "<row" | head -n 1000 | python indexing/query_autocomplete.py

Start web server
================
$ cd typeahead/ && python server.py

Point your browser to http://localhost:8080
